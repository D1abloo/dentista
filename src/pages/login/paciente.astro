---
const next = Astro.url.searchParams.get('next') ?? '/paciente'
const safeNext = next.startsWith('/') ? next : '/paciente'

return Astro.redirect(`/login?next=${encodeURIComponent(safeNext)}`)
---
