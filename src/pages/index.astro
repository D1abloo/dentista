---
import PublicLayout from '@/layouts/PublicLayout.astro';
import { LandingPage } from '@/components/DentistaApp';
import { landingStructuredDataGraph } from '@/lib/seo/landingStructuredData';
import { seoLayoutProps } from '@/lib/seo/layoutProps';

const seo = seoLayoutProps('/');
---
<PublicLayout {...seo} structuredData={landingStructuredDataGraph}>
  <LandingPage client:load />
</PublicLayout>
