---
/** Frontend v2 — CSS único + Tailwind (sin hojas legacy). */
import '../styles/global.css';
import '../styles/v2/index.css';
import { AiWidgetV2 } from '@/frontend/features/ai/AiWidgetV2';
import BaseLayout from './BaseLayout.astro';

const props = Astro.props;
---
<BaseLayout {...props}>
  <slot />
  <AiWidgetV2 slot="body-end" client:load />
</BaseLayout>
