---
/** Layout portales v2 — admin, paciente, platform, login. */
import '../styles/global.css';
import '../styles/v2/index.css';
import { isPrivatePath } from '@/lib/seo/siteConfig';
import BaseLayout from './BaseLayout.astro';

const props = Astro.props;
const isPrivate = isPrivatePath(Astro.url.pathname);
---
<BaseLayout {...props}>
  <slot />
</BaseLayout>
